<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/app_bg"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/group_manager_add"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        android:background="@drawable/mm_listitem"
        android:clickable="true"
        android:drawableLeft="@drawable/groupmanager_add_icon"
        android:drawablePadding="6dip"
        android:gravity="center_vertical"
        android:onClick="onAddGroupBtnClick"
        android:paddingLeft="9dp"
        android:text="@string/str_add_group"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

    <ListView
        android:id="@+id/group_manager_list"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@null"
        android:listSelector="@drawable/mm_listitem" />

</LinearLayout>